spring.application.name=libraryManager

# URL de connexion PostgreSQL (port 5432 par défaut)
spring.datasource.url=jdbc:postgresql://localhost:5432/librarymanager
spring.datasource.username=postgres
spring.datasource.password=regis@2007
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Correction du nom du dialecte
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect